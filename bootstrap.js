// Haven's Reach — final startup activation
// Render only after every content and behavior module has registered its data.
// This keeps saved games at expansion locations safe during a full page reload.

render();
